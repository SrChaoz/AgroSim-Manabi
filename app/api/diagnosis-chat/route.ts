import { NextRequest, NextResponse } from "next/server";
import { openai } from "@/lib/openai";
import { diagnosisChatJsonSchema, diagnosisChatSchema } from "@/lib/schemas";
export async function POST(request: NextRequest) { try { if (!process.env.OPENAI_API_KEY) return NextResponse.json({ error: "Configura OPENAI_API_KEY para continuar el diagnóstico." }, { status: 503 }); const { diagnosis, messages } = await request.json(); const completion = await openai.chat.completions.create({ model: "gpt-4o-mini", response_format: { type: "json_schema", json_schema: diagnosisChatJsonSchema }, messages: [{ role: "system", content: `Eres un asistente fitosanitario de Manabí. Responde preguntas solo sobre este diagnóstico: ${JSON.stringify(diagnosis)}. Da recomendaciones breves y prudentes; no sustituyas inspección ni prescripción profesional.` }, ...messages.map((message: { role: "user" | "assistant"; content: string }) => ({ role: message.role, content: message.content }))] }); return NextResponse.json(diagnosisChatSchema.parse(JSON.parse(completion.choices[0]?.message.content ?? "{}"))); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "No fue posible continuar la conversación." }, { status: 400 }); } }
