import { forwardRef, type ButtonHTMLAttributes } from "react"; import { cn } from "@/lib/utils";
export const Button = forwardRef<HTMLButtonElement, ButtonHTMLAttributes<HTMLButtonElement> & { variant?: "default" | "outline" }>(function Button({ className, variant = "default", ...props }, ref) { return <button ref={ref} className={cn("inline-flex items-center justify-center rounded-xl px-4 py-2.5 text-sm font-semibold transition disabled:cursor-not-allowed disabled:opacity-50", variant === "default" ? "bg-earth-700 text-white hover:bg-earth-900" : "border border-earth-100 bg-white text-earth-700 hover:bg-earth-50", className)} {...props} />; });
