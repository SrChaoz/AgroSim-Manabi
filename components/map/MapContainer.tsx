"use client";
import { MapContainer as LeafletMap, TileLayer, Polygon, Popup } from "react-leaflet"; import "leaflet/dist/leaflet.css";
import { PolygonDrawer } from "./PolygonDrawer"; import { formatHectares } from "@/lib/utils"; import type { Parcel, Coordinate } from "@/types";
export function MapCanvas({ parcel, onChange }: { parcel: Parcel | null; onChange: (points: Coordinate[]) => void }) { const points = parcel?.coordinates ?? []; return <div className="h-[410px] overflow-hidden rounded-2xl border border-earth-100"><LeafletMap center={[-1.0546, -80.4545]} zoom={10} className="h-full w-full" scrollWheelZoom><TileLayer attribution='&copy; OpenStreetMap contributors' url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" /><PolygonDrawer onPoint={(point) => onChange([...points, point])} />{points.length >= 3 && <Polygon positions={points.map((p) => [p.lat, p.lng])} pathOptions={{ color: "#31553a", fillColor: "#7faa65", fillOpacity: .35 }}><Popup>Parcela: {formatHectares(parcel?.areaHa ?? 0)}</Popup></Polygon>}</LeafletMap></div>; }
