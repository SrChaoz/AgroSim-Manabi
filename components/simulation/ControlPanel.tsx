"use client";
import { Droplets, CloudRain, Sprout } from "lucide-react"; import { Card } from "@/components/ui/card"; import { Slider } from "@/components/ui/slider"; import type { SimulationInput } from "@/types";
const selectStyle = "mt-2 w-full rounded-xl border border-earth-100 bg-earth-50 p-2.5 text-sm text-earth-900 outline-none";
export function ControlPanel({ input, onChange }: { input: SimulationInput; onChange: (input: SimulationInput) => void }) { return <Card className="space-y-5"><div><h2 className="font-bold text-earth-900">Escenario de cultivo</h2><p className="text-sm text-slate-500">Ajusta las condiciones de tu parcela.</p></div><label className="block text-sm font-medium"><span className="flex items-center gap-2"><Sprout size={16}/> Cultivo</span><select value={input.crop} onChange={(e) => onChange({ ...input, crop: e.target.value })} className={selectStyle}>{["Cacao", "Café", "Plátano", "Maíz", "Yuca"].map(x => <option key={x}>{x}</option>)}</select></label><label className="block text-sm font-medium">Tipo de suelo<select value={input.soilType} onChange={(e) => onChange({ ...input, soilType: e.target.value })} className={selectStyle}>{["Arcilloso", "Franco", "Arenoso"].map(x => <option key={x}>{x}</option>)}</select></label><label className="block text-sm font-medium"><span className="flex items-center gap-2"><CloudRain size={16}/> Lluvias</span><select value={input.rainfallScenario} onChange={(e) => onChange({ ...input, rainfallScenario: e.target.value })} className={selectStyle}><option>Estación habitual</option><option>Déficit de lluvias</option><option>Lluvias intensas</option></select></label><div><div className="mb-3 flex items-center justify-between text-sm font-medium"><span className="flex items-center gap-2"><Droplets size={16}/> Cambio de riego</span><span className="text-earth-700">{input.irrigationChange > 0 ? "+" : ""}{input.irrigationChange}%</span></div><Slider value={[input.irrigationChange]} onValueChange={([irrigationChange]) => onChange({ ...input, irrigationChange })} /></div></Card>; }
